# Great Games -- Change Log (2026-03-27)

Branch: `additionalStats`

This file captures the multi-sport documentation rollup completed on 2026-03-27, with focus on NBA and NHL additions now present in the current branch.

## Summary

- Documented the app as a true multi-sport shell (MLB, NBA, NHL) instead of MLB-only.
- Captured sport switcher, URL/localStorage sport persistence, and dynamic league bootstrapping.
- Documented NBA ESPN scoreboard/standings integration and compact NBA ranking model.
- Documented NHL ESPN scoreboard/standings integration with hockey-specific ranking signals.
- Linked current docs to all daily logs and added this new entry for ongoing tracking.

## Changes Documented In This Rollup

### 1) Multi-Sport Shell

- Added clear documentation for shell routing in `js/app.js`:
	- supported sports: `mlb`, `nba`, `nhl`
	- URL override: `?sport=`
	- persisted mode: `great-games-sport` in `localStorage`
	- dynamic imports of `js/mlb/app.js`, `js/nba/app.js`, and `js/nhl/app.js`
- Captured shell-level branding and modal-label switching by sport.

### 2) NBA Addition (Current Runtime)

- Confirmed and documented ESPN-only NBA data source in current code.
- Documented endpoints:
	- scoreboard: `/apis/site/v2/sports/basketball/nba/scoreboard?dates=YYYYMMDD&limit=200`
	- standings: `/apis/v2/sports/basketball/nba/standings?season=YYYY`
- Documented parser/ranker behavior:
	- mapped result-set contract (`GameHeader`, `LineScore`, `GameInfo`)
	- compact ranking criteria (`closeGames`, `leadChanges`, `extraInnings`)
	- scoreboard-first mode with no enhancement rows

### 3) NHL Addition (Current Runtime)

- Documented ESPN-based NHL scoreboard/standings integration.
- Documented parser extensions for hockey stats:
	- shots
	- saves
	- power-play goals
- Documented NHL ranking focus:
	- one-goal tension
	- momentum swings
	- overtime/shootout drama
	- goalie workload proxy

### 4) Shared UX Features Across Sports

- Documented shared table-first experience and spoiler-safe score reveal flow.
- Documented shared toolbar controls (`Filter`, `Standings`, `Ranking`) with sport-specific toggle visibility.
- Documented period-filter behavior (lookback days, team multi-select, minimum stars) in current shell.

### 5) Documentation Alignment

- Rewrote `readme.md` to align with current multi-sport architecture.
- Rewrote `documentation/system.md` to describe shell + per-sport modules accurately.
- Added this log to the recent updates list.

## Files Updated During This Chat

- `readme.md`
	- Reframed project scope to MLB + NBA + NHL.
	- Added provider, ranking, and behavior summaries per sport.
	- Added current documentation links including this file.
- `documentation/system.md`
	- Replaced MLB-only system description with multi-sport architecture and runtime flow.
	- Added sectioned breakdown for shell, MLB, NBA, NHL modules and criteria.
- `documentation/changes-2026-03-27.md`
	- Added this rollup entry.

## Verification Notes

- Documentation statements were verified against current source modules:
	- `js/app.js`
	- `js/nba/api.js`, `js/nba/parser.js`, `js/nba/ranker.js`, `js/nba/ui.js`
	- `js/nhl/api.js`, `js/nhl/parser.js`, `js/nhl/ranker.js`, `js/nhl/ui.js`
- Existing older logs (`2026-03-04`, `2026-03-05`) were preserved and linked.

